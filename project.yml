name: VidEdit

options:
  bundleIdPrefix: com.justjohn12345
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true
  groupSortPosition: top

settings:
  base:
    MARKETING_VERSION: 0.1.0
    CURRENT_PROJECT_VERSION: 1
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ARCHS: arm64
    CLANG_CXX_LANGUAGE_STANDARD: c++20
    CLANG_CXX_LIBRARY: libc++
    CLANG_ENABLE_OBJC_ARC: YES
    CLANG_ENABLE_MODULES: YES
    GCC_C_LANGUAGE_STANDARD: gnu17
    SWIFT_VERSION: "5.0"
    SWIFT_STRICT_CONCURRENCY: minimal
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    DEAD_CODE_STRIPPING: YES
    # Ad hoc signing so xcodebuild works headless without a developer account.
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Manual
    CODE_SIGNING_ALLOWED: YES
    CODE_SIGNING_REQUIRED: YES
    DEVELOPMENT_TEAM: ""
    ENABLE_HARDENED_RUNTIME: NO
    # FFmpeg is found through the system-header path so its headers don't add
    # warnings to our build; our own code keeps the full warning set.
    FFMPEG_ROOT: $(SRCROOT)/ThirdParty/ffmpeg
    SYSTEM_HEADER_SEARCH_PATHS: $(inherited) $(SRCROOT)/ThirdParty $(FFMPEG_ROOT)/include
    CLANG_WARN_DOCUMENTATION_COMMENTS: YES
    GCC_WARN_SHADOW: YES
    CLANG_WARN_IMPLICIT_SIGN_CONVERSION: NO
    GCC_WARN_64_TO_32_BIT_CONVERSION: YES

targets:
  VidEditEngine:
    type: framework
    platform: macOS
    sources:
      - path: Engine
        excludes:
          - "Facade/**"
          - "**/.gitkeep"
        headerVisibility: project
      - path: Engine/Facade
        headerVisibility: public
      # Stage the FFmpeg dylibs next to the framework in the build products
      # directory so the framework's @loader_path/../../.. rpath resolves them when
      # running from DerivedData (EngineTests). The directory must exist when
      # `xcodegen generate` runs, i.e. run Scripts/build-ffmpeg.sh first.
      - path: ThirdParty/ffmpeg/lib
        includes:
          - "lib*.*.dylib"
        buildPhase:
          copyFiles:
            destination: productsDirectory
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.justjohn12345.videdit.engine
        PRODUCT_NAME: VidEditEngine
        GENERATE_INFOPLIST_FILE: YES
        DEFINES_MODULE: YES
        SKIP_INSTALL: YES
        INSTALL_PATH: "@rpath"
        DYLIB_INSTALL_NAME_BASE: "@rpath"
        VERSIONING_SYSTEM: apple-generic
        LIBRARY_SEARCH_PATHS: $(inherited) $(FFMPEG_ROOT)/lib
        OTHER_LDFLAGS: $(inherited) -lavformat -lavcodec -lswscale -lswresample -lavutil
        # @loader_path is VidEditEngine.framework/Versions/A, so ../../.. is the directory
        # that contains the framework: Contents/Frameworks inside VidEdit.app, or the
        # build products directory in DerivedData. The FFmpeg dylibs are placed next to
        # the framework in both (see the copy-files phases for ThirdParty/ffmpeg/lib in
        # this target and in VidEdit).
        LD_RUNPATH_SEARCH_PATHS: $(inherited) @executable_path/../Frameworks @loader_path/Frameworks @loader_path/../../..
        MTL_LANGUAGE_REVISION: Metal30
        MTL_ENABLE_DEBUG_INFO: INCLUDE_SOURCE
    configs:
      Release:
        MTL_ENABLE_DEBUG_INFO: NO
    dependencies:
      - sdk: AVFoundation.framework
      - sdk: CoreMedia.framework
      - sdk: CoreVideo.framework
      - sdk: VideoToolbox.framework
      - sdk: Metal.framework
      - sdk: MetalKit.framework
      - sdk: MetalPerformanceShaders.framework
      - sdk: Accelerate.framework
      - sdk: AVFAudio.framework
      - sdk: AudioToolbox.framework
      - sdk: CoreImage.framework
      - sdk: IOSurface.framework
      - sdk: ImageIO.framework
      - sdk: CoreGraphics.framework
      - sdk: UniformTypeIdentifiers.framework

  EngineTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: EngineTests
        excludes:
          - "**/.gitkeep"
    dependencies:
      - target: VidEditEngine
      - sdk: Metal.framework
      - sdk: CoreMedia.framework
      - sdk: CoreVideo.framework
      - sdk: VideoToolbox.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.justjohn12345.videdit.EngineTests
        GENERATE_INFOPLIST_FILE: YES
        # EngineTests.xctest/Contents/MacOS/../../.. is the build products directory.
        LD_RUNPATH_SEARCH_PATHS: $(inherited) @loader_path/../Frameworks @loader_path/../../..

  VidEdit:
    type: application
    platform: macOS
    sources:
      - path: App
        excludes:
          - "**/.gitkeep"
      # Embed the FFmpeg dylibs in VidEdit.app/Contents/Frameworks and sign them on
      # copy (LGPL: the libraries stay dynamically linked and replaceable).
      - path: ThirdParty/ffmpeg/lib
        includes:
          - "lib*.*.dylib"
        attributes:
          - CodeSignOnCopy
        buildPhase:
          copyFiles:
            destination: frameworks
    info:
      path: App/Resources/Info.plist
      properties:
        CFBundleDisplayName: VidEdit
        CFBundleName: VidEdit
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSApplicationCategoryType: public.app-category.video
        LSMinimumSystemVersion: $(MACOSX_DEPLOYMENT_TARGET)
        NSHumanReadableCopyright: ""
        CFBundleDocumentTypes:
          - CFBundleTypeName: VidEdit Project
            CFBundleTypeRole: Editor
            LSHandlerRank: Owner
            LSItemContentTypes:
              - com.justjohn12345.videdit.project
        UTExportedTypeDeclarations:
          - UTTypeIdentifier: com.justjohn12345.videdit.project
            UTTypeDescription: VidEdit Project
            UTTypeConformsTo:
              - public.json
            UTTypeTagSpecification:
              public.filename-extension:
                - videdit
              public.mime-type:
                - application/x-videdit+json
    entitlements:
      path: App/Resources/VidEdit.entitlements
      properties:
        com.apple.security.app-sandbox: true
        com.apple.security.files.user-selected.read-write: true
        com.apple.security.files.bookmarks.app-scope: true
    dependencies:
      - target: VidEditEngine
        embed: true
        codeSign: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.justjohn12345.videdit
        PRODUCT_NAME: VidEdit
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        LD_RUNPATH_SEARCH_PATHS: $(inherited) @executable_path/../Frameworks
        ENABLE_PREVIEWS: YES
        COMBINE_HIDPI_IMAGES: YES

  AppTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: AppTests
        excludes:
          - "**/.gitkeep"
    dependencies:
      - target: VidEdit
      - target: VidEditEngine
        embed: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.justjohn12345.videdit.AppTests
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/VidEdit.app/Contents/MacOS/VidEdit
        BUNDLE_LOADER: $(TEST_HOST)

schemes:
  VidEdit:
    build:
      targets:
        VidEdit: all
        VidEditEngine: all
        EngineTests: [test]
        AppTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      coverageTargets:
        - VidEdit
        - VidEditEngine
      targets:
        - EngineTests
        - AppTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

  EngineTests:
    build:
      targets:
        VidEditEngine: all
        EngineTests: [test]
    test:
      config: Debug
      targets:
        - EngineTests

  AppTests:
    build:
      targets:
        VidEdit: all
        AppTests: [test]
    test:
      config: Debug
      targets:
        - AppTests
